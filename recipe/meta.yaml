{% set name = "anaconda-ai" %}
{% set version = "0.5.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/anaconda_ai-{{ version }}.tar.gz
  sha256: e73c957f825e6eee80eae308da311893add04e9f60fbe67c0b02cffd4453dd0d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - hatch-vcs >=0.3
    - setuptools-scm >=7.1
    - pip
  run:
    - python >={{ python_min }}
    - anaconda-auth >=0.13.0
    - anaconda-cli-base >=0.8.1
    - rich
    - pydantic >=2
    - typer
    - ruamel.yaml
    - openai
    - platformdirs
    - packaging
  run_constrained:
    - langchain-openai >=0.2.8
    - llm >=0.22
    - mcp >=1.26
    - pydantic-ai >=1.50

test:
  imports:
    - anaconda_ai
    - anaconda_ai.cli
    - anaconda_ai.clients
    - anaconda_ai.config
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  summary: Download, launch, and integrate AI models curated by Anaconda
  license: BSD-3-Clause
  license_file: LICENSE
  home: https://anaconda.cloud

extra:
  recipe-maintainers:
    - AlbertDeFusco
    - nbeerbower
